# API base URL — leave empty for same-origin (recommended for single-server deploy + local dev proxy)
# Set only when the frontend and backend are on different domains, e.g. https://api.example.com
VITE_API_URL=

# Vite dev-server proxy target (local development only)
VITE_API_PROXY_TARGET=http://localhost:3000
